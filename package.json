{
  "name": "nistjs",
  "version": "1.0.2",
  "description": "",
  "main": "index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "init": "cp -R ./builder/src",
    "build": "rm -rf ./dist && ./node_modules/.bin/tsc && rm -rf ./dist/builder",
    "format": "prettier --write \"src/**/*.ts\"",
    "lint": "tslint -p tsconfig.json",
    "prepare": "npm run build",
    "prepublishOnly": "npm test && npm run lint",
    "preversion": "npm run lint",
    "version": "npm run format && git add -A src",
    "postversion": "git push && git push --tags"
  },
  "author": "moamza",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.11.17",
    "prettier": "^2.8.1",
    "test": "",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^4.9.4"
  },
  "files": [
    "dist/**/*"
  ],
  "dependencies": {}
}
